/**
 *
 * {{ properCase name }} Container
 * created by generator
 *
 */
{{#if wantWX}}

/* global wx */
{{/if}}
// third party => components => actions => reducers => assets => other
// other 中私有组件放在最前面
import Taro, { Component } from '@tarojs/taro'
import { View, Text } from '@tarojs/components'
import { connect } from '@tarojs/redux'
import PropTypes from 'prop-types'
import { EPage } from 'eft-cool-taro-ui'
{{#if wantHeader}}

import CustomHeader from 'components/CustomHeader'
{{/if}}

import './index.scss'

@connect((state) => (
  state
), (dispatch) => ({
  dispatch
})
)

class {{ properCase name }} extends Component {
  // constructor(props) {
  //   super(props)
  //   this.state = {}
  // }

  render() {
    const { generateText } = this.props
    {{#if wantWX}}
    console.log(wx)
    {{/if}}
    return (
      <EPage
        className='{{ kebabCase name }}-container'
        {{#if wantHeader}}
        renderHeader={<CustomHeader isShowBackHome />}
        {{/if}}
      >
        <View className='default-page-content'>
          <Text className='generate-text'>This page name is {{ sentenceCase name }}</Text>
          <Text className='generate-text'>{generateText}</Text>
        </View>
      </EPage>
    )
  }
}

{{ properCase name }}.defaultProps = {
  generateText: 'This generate page'
}

/*
 * 父级 props
 * 自身 state
 * 自身 dispatch function
 */
{{ properCase name }}.propTypes = {
  generateText: PropTypes.string
}

export default {{ properCase name }}
