/*
 *
 * {{ properCase name }} reducer
 * created by generator
 *
 */

 import {
   DEFAULT_ACTION,
 } from 'constants/{{ camelCase name }}'

const INITIAL_STATE = {
  generateText: 'welcome'
}

export default function {{ camelCase name }}(state = INITIAL_STATE, action) {
  switch (action.type) {
    case DEFAULT_ACTION:
      return state;
    default:
      return state;
  }
}
