/**
 *
 * {{ properCase name }} Component
 * created by generator
 *
 */
import Taro, { Component } from '@tarojs/taro'
import { View, Text } from '@tarojs/components'
import PropTypes from 'prop-types'

{{#if isPageComponent}}
import './{{ camelCase name }}.scss'
{{else}}
import './index.scss'
{{/if}}

export class {{ properCase name }} extends Component {
  // constructor(props) {
  //   super(props)
  //   this.state = {}
  // }

  render() {
    const { generateComponentText } = this.props;
    return (
      <View className='{{ kebabCase name }}-container'>
        <Text className='generate-text'>This component name is {{ sentenceCase name }}</Text>
        <Text className='generate-text'>{generateComponentText}</Text>
      </View>
    )
  }
}

{{ properCase name }}.defaultProps = {
  generateComponentText: 'welcome',
};

{{ properCase name }}.propTypes = {
  generateComponentText: PropTypes.string,
};

export default {{ properCase name }}
